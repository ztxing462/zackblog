# 腾讯云服务器Nginx配置示例
# 将此配置文件保存到服务器的 /etc/nginx/conf.d/ 目录下，并重启Nginx服务

server {
    # 监听80端口（HTTP）
    listen 80;
    # 配置您的域名
    server_name zengtx.cloud www.zengtx.cloud;
    
    # 设置网站根目录，指向Astro构建后的dist目录
    root /path/to/your/blog/dist;
    index index.html;
    
    # 配置静态文件缓存
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, max-age=2592000";
    }
    
    # 处理SPA路由，确保直接访问子页面时能正确加载
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 自定义404页面
    error_page 404 /404.html;
    
    # 记录访问日志
    access_log /var/log/nginx/your-domain-access.log;
    error_log /var/log/nginx/your-domain-error.log;
}

# 如果需要配置HTTPS，请参考以下配置（需要先申请SSL证书）
#server {
#    listen 443 ssl;
#    server_name zengtx.cloud www.zengtx.cloud;
#    
#    # SSL证书配置
#    ssl_certificate /path/to/your/ssl/certificate.crt;
#    ssl_certificate_key /path/to/your/ssl/private.key;
#    
#    # SSL优化配置
#    ssl_protocols TLSv1.2 TLSv1.3;
#    ssl_prefer_server_ciphers off;
#    ssl_session_timeout 1d;
#    ssl_session_cache shared:SSL:10m;
#    ssl_session_tickets off;
#    
#    # HSTS配置
#    add_header Strict-Transport-Security "max-age=63072000" always;
#    
#    # 其他配置与HTTP部分相同
#    root /path/to/your/blog/dist;
#    index index.html;
#    
#    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
#        expires 30d;
#        add_header Cache-Control "public, max-age=2592000";
#    }
#    
#    location / {
#        try_files $uri $uri/ /index.html;
#    }
#    
#    error_page 404 /404.html;
#    
#    access_log /var/log/nginx/your-domain-ssl-access.log;
#    error_log /var/log/nginx/your-domain-ssl-error.log;
#}

# 重定向HTTP到HTTPS（如果配置了HTTPS）
#server {
#    listen 80;
#    server_name zengtx.cloud www.zengtx.cloud;
#    return 301 https://$server_name$request_uri;
#}